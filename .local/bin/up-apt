#!/usr/bin/env bash

sudo apt update

list=(
    "build-essential"
    "git"
    "net-tools"
    "curl"
    "wget"
    "xclip"
)
for item in "${list[@]}"; do
    echo "Installing $item..."
    sudo apt install $item
done

sudo apt upgrade
