#!/bin/bash

if [ $# -gt 0 ]; then
    input="$*"
else
    input=$(cat -)
fi

output=$(echo "$input" | tr '[:upper:]' '[:lower:]')
output=$(echo "$output" | sed -E 's/[^a-zA-Z0-9-]+/-/g')
output=$(echo "$output" | sed -E 's/^-+//g')
output=$(echo "$output" | sed -E 's/-+$//g')

echo "$output"
