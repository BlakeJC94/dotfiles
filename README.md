# My dotfiles and such

WIP: install.sh

# Install

When install.sh is done and tested, running install.sh will install all development
environment dependencies.

1. `git clone https://github.com/claytod5/dotfiles`
2. `cd dotfiles/`
3. `chmod +x install.sh`
4. `./install.sh`
